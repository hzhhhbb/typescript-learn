# 异步与 Promise 课程完成：语言核心里程碑达成

用户完成第八课（异步与 Promise）。已掌握：Promise&lt;T&gt; 与 Task&lt;T&gt; 的对照、async/await（C# 直接迁移）、Promise.all（Task.WhenAll）、fetch 的错误模型（HTTP 4xx/5xx 不抛异常、需检查 res.ok——与 HttpClient 相反）。第八课包含真实网络请求示例（jsonplaceholder），全部编译运行验证通过。

**Evidence**: "ok, next one"推进（晚间完成，随后休息）；async.ts 示例与两个错误路径挑战均由 agent 验证。

**Implications**: 语言核心课程（0001-0008）全部完成，用户达到使命第一里程碑"读懂真实全栈代码"。下一阶段（0009 模块系统、0010 tsconfig/工程化、0011 实战项目）指向第二里程碑"自己从零搭项目"。用户学习节奏稳定（约一课/次会话），晚间学习——后续课程可保持现有短课制与对照风格。
