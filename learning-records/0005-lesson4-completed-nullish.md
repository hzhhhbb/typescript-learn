# 空值处理课程完成：null/undefined、严格空检查、可选链与空值合并

用户完成第四课（null、undefined 与严格空检查）。测验结果未明确报告，但课程推进顺畅。已接触：undefined（系统给的没有）与 null（人给的没有）的区分、strictNullChecks 下 string 不含 null、可选链/空值合并（C# 直接迁移）、非空断言的运行时风险、?? vs || 的假值差异。环境确认使用 TypeScript 7（新原生编译器），新增 TS2869/TS2873 严格检查。

**Evidence**: 用户"ok, next one"推进（完成信号）；课程示例与两个挑战均已由 agent 验证可运行。

**Implications**: 用户的 C# 经验（?. 和 ??）使空值处理课学习曲线平缓，可正常进入泛型课（0005）。TS 7 新检查（TS2869/TS2873）已记录为环境特性，后续课程示例须在 TS 7 下验证通过（避免教程代码编译失败）。
