# 第一课核心概念掌握：类型擦除、推断与 strict 模式

用户完成第一课，三个测验全部答对（Q1 类型不匹配、Q2 类型擦除、Q3 类型推断），证明已理解：TS 类型是纯编译期概念且编译后擦除；类型推断与显式标注同等严格（`let x = "hello"` 后赋 number 报错）；能读懂 TS2322 错误的方向。本地环境已跑通（code/ 目录，npm + tsc + tsconfig + src/dist 结构），编译-报错-修复流程可用。

**Evidence**: 三个测验无错；环境搭建实际完成（此前 agent 验证过命令，用户报告"finish lesson one"）。

**Implications**: "类型是编译期的"心智模型已建立——结构类型课程（0002）可以在此基础上展开（结构匹配同样是编译期行为，运行时没有接口概念）。用户具备独立动手能力，练习可设计为"预测编译结果 + 在 code/ 中验证"。环境已就绪，后续课程可直接提供可运行示例文件。
