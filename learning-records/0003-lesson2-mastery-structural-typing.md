# 结构类型掌握：结构 vs 名义、多余属性检查、可选属性

用户完成第二课，四个测验全部答对（结构 vs 名义、多余属性检查的不对称性、可选属性语义、结构匹配是编译期行为）。已运行 structural.ts 示例，但"字面量 vs 变量"挑战未亲手验证——对 excess property check 的不对称性处于"知道但未亲手体验"状态。

**Evidence**: 四题全对；练习报告"做了示例，挑战没做"。

**Implications**: 第三课开头加入 1 分钟热身，让用户亲手触发一次 TS2353（spacing + 补上漏掉的亲手验证）。用户对"类型/接口是纯编译期概念"的心智模型已牢固，可放心教依赖此模型的收窄（narrowing）概念——收窄正是"编译器根据运行时检查更新编译期类型"的机制。
