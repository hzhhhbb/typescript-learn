# 函数与对象类型课程完成

用户完成第六课（函数与对象类型）。已掌握：函数类型注解 (a: number) => number（C# Func/委托对照）、回调参数类型、可选/默认/剩余参数、TS 重载（多个签名 + 一个实现）、readonly、索引签名。至此语言核心类型系统（类型擦除、结构类型、联合/收窄、空值、泛型、函数/对象类型）已基本完整。

**Evidence**: "ok, next one"推进；示例（MathOp、forEach 回调、greet、sum、pick 重载、Point readonly、StringMap）均由 agent 编译运行验证。

**Implications**: 用户已具备完整阅读真实 TS 类型代码的能力（前 6 课覆盖了类型系统全貌）。可进入类课（0007），重点讲 C# 迁移时的差异：参数属性、private 两种写法（编译期 vs 运行时）、implements 在结构类型下的意义、装饰器。0008 异步课后用户即达到"读懂真实全栈代码"的使命里程碑。
