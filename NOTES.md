# 教学笔记

## 用户偏好

- **概念对照为主**：每个概念先映射到 C#/Java 的已知概念，聚焦差异，不重复已懂内容。
- **每课 15-20 分钟**：短课制，一次一个紧凑主题。
- **课程中出现未讲过的语法时，必须用注释/标注说明**（用户明确要求，2025 年 8 月）：
  - 课程正文：用「全新概念」标注框 + 链接词汇表
  - code/ 示例文件：用行内注释标注关键点
  - 晦涩但暂不需要展开的语法：明确告知"现在只需知道存在，后课展开"
- **课程间引用前必须确认被引内容真的教过**（第九课教训，2025 年 8 月）：示例文件里出现的语法 ≠ 课程里讲过。引用 "第 X 课学的 Y" 之前，先 grep 确认 Y 确实在 X 课正文中出现过。
- **动手练习是核心**：每课必备 code/ 可运行示例 + 挑战（预测→运行→验证）。

## 环境事实

- Node.js v24、npm 11、TypeScript 7.0.2（新原生编译器，`tsc` 命令）
- TS 7 新增严格检查：TS2869（永不空的 ??）、TS2873（永远假的 ||），课程示例须在此环境下验证
- @types/node 26 中 process.env 索引签名为 string（非 string | undefined）
- 标准装饰器可用（target ES2022，无需 experimentalDecorators）
- 示例文件统一加 `export {}` 使其成为独立模块（避免全局作用域冲突）

## 工作区约定

- 课程编号 lessons/000N-<slug>.html，每个课程链接上/下一课 + 术语表
- 所有示例先在 code/src/ 真实编译运行验证，再写入课程
- 词汇表词条在课程涉及新术语时同步扩充
- 学习记录在用户完成课程（有证据）后写入
